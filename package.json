{
  "name": "sst-hfwa",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove",
    "console": "sst console",
    "test": "vitest run"
  },
  "devDependencies": {
    "@serverless-stack/cli": "^1.0.2",
    "@serverless-stack/resources": "^1.0.2",
    "@tsconfig/node14": "^1.0.1",
    "aws-cdk-lib": "2.20.0",
    "typescript": "^4.6.4",
    "vitest": "^0.10.5"
  },
  "workspaces": [
    "backend"
  ]
}
